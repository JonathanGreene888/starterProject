import React from 'react';
import GameSquare from '../GameSquare/GameSquare.component';

const GameBoard = () => {
    {
        for (let i = 0; i < 64; i++) {
            return <GameSquare />;
        }
    }
};

export default GameBoard;
